# platform 目录约定

- 这里放系统能力、厂商/ROM 适配、Shizuku、shell、设备能力等底层接入。
- 该层只暴露稳定的小接口或底层实现，不持有页面状态，不直接处理页面配置 UI。
- 与业务强绑定的组合逻辑优先留在 `feature/` 或 `action/`，不要把业务流程塞进平台能力层。
- 需要新增平台目录时，先按能力命名，例如 `freeform/`、`shizuku/`、`shell/`，不要按调用方业务命名。
