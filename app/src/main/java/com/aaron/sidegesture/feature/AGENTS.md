# feature 目录约定

- 这里放具体业务功能模块，例如快捷启动、快速工具、截图、任务切换、更新、手势运行时、动作面板和移屏。
- 功能模块内可以包含运行时 UI、状态、executor、manager、service 等与该功能强绑定的实现。
- 页面级配置默认仍放在 `ui/screen/`；只有功能整体迁入 `feature/` 时，才把对应配置 UI 一起收进功能目录。
- 不把通用系统能力、ROM 适配、Shizuku、shell 执行等平台能力放进这里；这类能力放 `platform/`。
- 新增功能目录前先明确职责边界，避免把多个无关功能塞进同一个目录。
